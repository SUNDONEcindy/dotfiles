return {
  "nvim-telescope/telescope.nvim",
  keys = {
    -- <leader>ff does the same thing and we want to use this in commander.lua
    { "<leader><space>", vim.NIL },
    -- {"<leader>snH", "Telescope notify<CR>", desc = "Notify history"}
  },
}
